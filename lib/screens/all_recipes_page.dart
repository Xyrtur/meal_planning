/*
 * 
 * 
 * Show list of recipes at first, entire list\
 * Common list to share
 * 
 * Searchbar logic > onSearchClicked (commonRecipeList)

 * OnTogglefilter button { 
 *  send toggled event  ofr specific filter
 * Uses filter bloc
 */
import 'package:flutter/material.dart';

class AllRecipesPage extends StatelessWidget {
  const AllRecipesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
